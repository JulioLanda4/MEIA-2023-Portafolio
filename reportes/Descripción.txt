Para el reto, exportamos los datos a un dataframe y redujimos su dimensionalidad de 64D a 2D utilizando tres métodos diferentes: PCA, t-SNE y UMAP. Dado que los datos no presentaban una relación lineal, decidimos utilizar t-SNE, ya que demostró brindar mejores resultados. Una vez que creamos el nuevo dataframe con las dimensiones reducidas, procedimos a detectar anomalías. Utilizamos cuatro métodos diferentes: K-means, KNN, LOF e Isolation Forest. Ajustamos los parámetros, como el número de vecinos o el porcentaje de contaminación, en cada método. Para KNN, aplicamos un algoritmo genético para encontrar los valores óptimos, pero debido a la falta de información detallada, los resultados no fueron muy satisfactorios. Al final, evaluamos los resultados obtenidos por los cuatro métodos y notamos que eran bastante similares entre sí. Identifiqué las anomalías en nuestro conjunto de datos basándome en estos resultados.